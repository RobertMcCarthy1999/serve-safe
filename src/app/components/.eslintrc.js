// .eslintrc.js
module.exports = {
  root: true,
  extends: ['next', 'next/core-web-vitals', 'eslint:recommended'],
  rules: {
    // Example: turn off rule if needed
    // 'no-console': 'warn',
  },
};
